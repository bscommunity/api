services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      POSTGRES_URL: "postgresql://<REMOTE_DB_HOST>:5432/<DB_NAME>"
      POSTGRES_USER: "<DB_USER>"
      POSTGRES_PASSWORD: "<DB_PASSWORD>"
    depends_on:
      - db
